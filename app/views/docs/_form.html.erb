<%= simple_form_for @doc do |f| %>

  <%= f.input :title, label: "Documentary Title" %>
  <%= f.input :description %>
  <%= f.input :director, label: "Director(s)" %>
  <%= f.button :submit %>

<% end %>
